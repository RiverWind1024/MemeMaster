# TREK 风格 UI 视觉改造实施计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 TREK 的视觉设计语言（indigo 强调色、TREK 色板、玻璃态、渐进圆角、统一动画）应用到 MemeHelper 全部 UI。

**Architecture:** 集中式主题层 `lib/core/theme/` 三个文件（app_animations / glass_container / app_theme），`app.dart` 接入两套主题；玻璃态只作用于底部导航栏、AppBar、弹窗/底部面板，内容卡片保持实心。

**Tech Stack:** Flutter 4.x / Material 3 / Dart 3.12。无新增依赖。

**Spec:** `docs/superpowers/specs/2026-08-19-trek-ui-design.md`

---

## Chunk 1: 主题基础设施

### Task 1: 创建动画常量 `app_animations.dart`

**Files:**
- Create: `lib/core/theme/app_animations.dart`

- [ ] **Step 1: 创建文件**

```dart
import 'package:flutter/animation.dart';

// 注：本文件常量为主题基础设施的一部分，供后续新增动画/过渡时统一引用（预留）。
// 当前版本尚未消费，避免各处硬编码散落的 duration/curve。

/// TREK 主缓动曲线，等价 cubic-bezier(.2, .7, .2, 1)
const kTrekEase = Cubic(0.2, 0.7, 0.2, 1.0);

/// TREK 快速弹出曲线，等价 cubic-bezier(.23, 1, .32, 1)
const kTrekEaseQuint = Cubic(0.23, 1.0, 0.32, 1.0);

/// 标准交互时长
const kTrekDuration = Duration(milliseconds: 220);
```

- [ ] **Step 2: 验证可编译**

Run: `flutter analyze lib/core/theme/app_animations.dart`
Expected: `No issues found!`

- [ ] **Step 3: Commit**

```bash
git add lib/core/theme/app_animations.dart
git commit -m "feat(theme): 新增 TREK 动画常量"
```

### Task 2: 创建玻璃组件 `glass_container.dart`

**Files:**
- Create: `lib/core/theme/glass_container.dart`

- [ ] **Step 1: 创建文件**

```dart
import 'dart:ui';

import 'package:flutter/material.dart';

/// TREK 风格毛玻璃容器：BackdropFilter blur + 半透明背景 + 高光描边 + 阴影
class GlassContainer extends StatelessWidget {
  final Widget child;
  final double blur;
  final double borderRadius;
  final bool showBorder;
  final EdgeInsetsGeometry padding;
  final Color? background;

  const GlassContainer({
    super.key,
    required this.child,
    this.blur = 22,
    this.borderRadius = 14,
    this.showBorder = true,
    this.padding = EdgeInsets.zero,
    this.background,
  });

  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context);
    final isDark = theme.brightness == Brightness.dark;
    final bg = background ??
        (isDark
            ? Colors.white.withValues(alpha: 0.06)
            : Colors.white.withValues(alpha: 0.7));

    return ClipRRect(
      borderRadius: BorderRadius.circular(borderRadius),
      child: BackdropFilter(
        filter: ImageFilter.blur(sigmaX: blur, sigmaY: blur),
        child: Container(
          padding: padding,
          decoration: BoxDecoration(
            color: bg,
            borderRadius: BorderRadius.circular(borderRadius),
            border: showBorder
                ? Border.all(color: Colors.white.withValues(alpha: 0.1))
                : null,
            boxShadow: [
              BoxShadow(
                color: Colors.black.withValues(alpha: 0.18),
                blurRadius: 32,
                offset: const Offset(0, 8),
              ),
            ],
          ),
          child: child,
        ),
      ),
    );
  }
}
```

- [ ] **Step 2: 验证可编译**

Run: `flutter analyze lib/core/theme/glass_container.dart`
Expected: `No issues found!`

- [ ] **Step 3: Commit**

```bash
git add lib/core/theme/glass_container.dart
git commit -m "feat(theme): 新增 TREK 毛玻璃容器组件"
```

### Task 3: 创建主题 `app_theme.dart`

**Files:**
- Create: `lib/core/theme/app_theme.dart`
- Test: `test/core/theme/app_theme_test.dart`

- [ ] **Step 1: 编写失败测试**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:mememaster/core/theme/app_theme.dart';

void main() {
  group('buildLightTheme', () {
    final theme = buildLightTheme();
    final cs = theme.colorScheme;

    test('强调色为 indigo #4F46E5', () {
      expect(cs.primary.toARGB32(), const Color(0xFF4F46E5).toARGB32());
    });

    test('主背景 surface 为白色 #FFFFFF', () {
      expect(cs.surface.toARGB32(), const Color(0xFFFFFFFF).toARGB32());
    });

    test('主文字 onSurface 为 #111827', () {
      expect(cs.onSurface.toARGB32(), const Color(0xFF111827).toARGB32());
    });
  });

  group('buildDarkTheme', () {
    final theme = buildDarkTheme();
    final cs = theme.colorScheme;

    test('主背景 surface 为深色 #121215', () {
      expect(cs.surface.toARGB32(), const Color(0xFF121215).toARGB32());
    });

    test('强调色为 indigo #6366F1', () {
      expect(cs.primary.toARGB32(), const Color(0xFF6366F1).toARGB32());
    });

    test('主文字 onSurface 为 #F4F4F5', () {
      expect(cs.onSurface.toARGB32(), const Color(0xFFF4F4F5).toARGB32());
    });
  });
}
```

> 注：`Color.toARGB32()` 是 Flutter 4.x API（替代旧的 `value`）。若 analyze 报错请改用 `.value`。

- [ ] **Step 2: 运行测试确认失败**

Run: `flutter test test/core/theme/app_theme_test.dart`
Expected: FAIL（`app_theme.dart` 不存在，编译错误）

- [ ] **Step 3: 创建主题实现**

```dart
import 'package:flutter/material.dart';

/// 浅色主题（TREK 风格：indigo 强调色 + 浅灰表面 + 白背景）
ThemeData buildLightTheme() {
  return _buildTheme(
    const ColorScheme.light(
      primary: Color(0xFF4F46E5),
      onPrimary: Colors.white,
      primaryContainer: Color(0xFFE0E7FF),
      onPrimaryContainer: Color(0xFF312E81),
      secondary: Color(0xFF4F46E5),
      onSecondary: Colors.white,
      surface: Color(0xFFFFFFFF),
      onSurface: Color(0xFF111827),
      onSurfaceVariant: Color(0xFF374151),
      surfaceContainerLow: Color(0xFFF8FAFC),
      surfaceContainerHigh: Color(0xFFEFF2F5),
      surfaceContainerHighest: Color(0xFFF1F5F9),
      outline: Color(0xFF6B7280),
      outlineVariant: Color(0xFFD1D5DB),
      error: Color(0xFFDC2626),
      onError: Colors.white,
    ),
    Brightness.light,
  );
}

/// 深色主题（TREK 风格：近黑背景 #121215 + 深灰表面）
ThemeData buildDarkTheme() {
  return _buildTheme(
    const ColorScheme.dark(
      primary: Color(0xFF6366F1),
      onPrimary: Colors.white,
      primaryContainer: Color(0xFF3730A3),
      onPrimaryContainer: Color(0xFFE0E7FF),
      secondary: Color(0xFF6366F1),
      onSecondary: Colors.white,
      surface: Color(0xFF121215),
      onSurface: Color(0xFFF4F4F5),
      onSurfaceVariant: Color(0xFFD4D4D8),
      surfaceContainerLow: Color(0xFF1A1A1E),
      surfaceContainerHigh: Color(0xFF26262B),
      surfaceContainerHighest: Color(0xFF303036),
      outline: Color(0xFFA1A1AA),
      outlineVariant: Color(0xFF3F3F46),
      error: Color(0xFFEF4444),
      onError: Colors.white,
    ),
    Brightness.dark,
  );
}

ThemeData _buildTheme(ColorScheme scheme, Brightness brightness) {
  final isDark = brightness == Brightness.dark;
  final base = ThemeData(useMaterial3: true, colorScheme: scheme);

  return base.copyWith(
    scaffoldBackgroundColor: scheme.surface,
    textTheme: base.textTheme
        .apply(bodyColor: scheme.onSurface, displayColor: scheme.onSurface)
        .copyWith(
          titleLarge: base.textTheme.titleLarge?.copyWith(
            fontWeight: FontWeight.w600,
            color: scheme.onSurface,
          ),
          titleMedium: base.textTheme.titleMedium?.copyWith(
            color: scheme.onSurfaceVariant,
          ),
          bodySmall: base.textTheme.bodySmall?.copyWith(
            color: scheme.outline,
          ),
        ),
    appBarTheme: AppBarTheme(
      backgroundColor: Colors.transparent,
      elevation: 0,
      scrolledUnderElevation: 0,
      foregroundColor: scheme.onSurface,
      centerTitle: false,
    ),
    navigationBarTheme: NavigationBarThemeData(
      backgroundColor: Colors.transparent,
      elevation: 0,
      height: 68,
      indicatorColor: scheme.primary.withValues(alpha: isDark ? 0.25 : 0.14),
      labelTextStyle: WidgetStatePropertyAll(
        TextStyle(fontSize: 12, color: scheme.onSurfaceVariant),
      ),
      iconTheme: WidgetStateProperty.resolveWith(
        (states) => states.contains(WidgetState.selected)
            ? IconThemeData(color: scheme.primary)
            : IconThemeData(color: scheme.onSurfaceVariant),
      ),
    ),
    cardTheme: CardThemeData(
      elevation: 0,
      color: scheme.surfaceContainerLow,
      margin: EdgeInsets.zero,
      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(18)),
    ),
    dialogTheme: DialogThemeData(
      backgroundColor: isDark
          ? const Color(0xF01A1A1E)
          : const Color(0xF2FFFFFF),
      elevation: 12,
      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(22)),
    ),
    bottomSheetTheme: BottomSheetThemeData(
      backgroundColor: isDark
          ? const Color(0xF21A1A1E)
          : const Color(0xF5FFFFFF),
      elevation: 0,
      shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.vertical(top: Radius.circular(28)),
      ),
      modalBackgroundColor: isDark
          ? const Color(0xF21A1A1E)
          : const Color(0xF5FFFFFF),
    ),
    inputDecorationTheme: InputDecorationTheme(
      filled: true,
      fillColor: scheme.surfaceContainerLow,
      contentPadding: const EdgeInsets.symmetric(horizontal: 16, vertical: 14),
      border: OutlineInputBorder(
        borderRadius: BorderRadius.circular(14),
        borderSide: BorderSide.none,
      ),
      enabledBorder: OutlineInputBorder(
        borderRadius: BorderRadius.circular(14),
        borderSide: BorderSide(color: scheme.outlineVariant),
      ),
      focusedBorder: OutlineInputBorder(
        borderRadius: BorderRadius.circular(14),
        borderSide: BorderSide(color: scheme.primary, width: 1.5),
      ),
    ),
    filledButtonTheme: FilledButtonThemeData(
      style: FilledButton.styleFrom(
        backgroundColor: scheme.primary,
        foregroundColor: scheme.onPrimary,
        shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(10)),
      ),
    ),
    textButtonTheme: TextButtonThemeData(
      style: TextButton.styleFrom(
        shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(10)),
      ),
    ),
    chipTheme: base.chipTheme.copyWith(
      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(10)),
    ),
    tabBarTheme: TabBarThemeData(
      indicatorColor: scheme.primary,
      labelColor: scheme.primary,
      unselectedLabelColor: scheme.onSurfaceVariant,
      dividerColor: Colors.transparent,
    ),
    snackBarTheme: SnackBarThemeData(
      behavior: SnackBarBehavior.floating,
      backgroundColor: isDark ? const Color(0xFF1A1A1E) : const Color(0xFF111827),
      contentTextStyle: TextStyle(color: isDark ? const Color(0xFFF4F4F5) : Colors.white),
      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(14)),
    ),
    dividerTheme: DividerThemeData(
      color: scheme.outlineVariant.withValues(alpha: 0.6),
    ),
  );
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `flutter test test/core/theme/app_theme_test.dart`
Expected: PASS（6 个用例：浅色 3 + 深色 3）

- [ ] **Step 5: Commit**

```bash
git add lib/core/theme/app_theme.dart test/core/theme/app_theme_test.dart
git commit -m "feat(theme): 新增 TREK 风格浅/深主题"
```

### Task 4: app.dart 接入新主题

**Files:**
- Modify: `lib/app.dart:274-287`

- [ ] **Step 1: 修改主题引用**

在 `lib/app.dart` 顶部 import 区追加：

```dart
import 'core/theme/app_theme.dart';
```

将 build 方法中的 `theme:` / `darkTheme:` 替换：

```dart
      theme: buildLightTheme(),
      darkTheme: buildDarkTheme(),
      themeMode: themeMode,
```

删除原来的 `ThemeData(colorScheme: ColorScheme.fromSeed(...))` 两段。

- [ ] **Step 2: 验证编译 + 测试**

Run: `flutter analyze lib/app.dart`
Expected: `No issues found!`

Run: `flutter test test/core/theme/app_theme_test.dart`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add lib/app.dart
git commit -m "feat(theme): app 接入 TREK 风格浅/深主题"
```

---

## Chunk 2: 玻璃态页面改造

> **与设计文档的偏差说明**（实现中的刻意收窄，视觉验收确认后可保持）：
> - 玻璃容器省略 `saturate(1.7)` 饱和度增强（Flutter `ImageFilter` 无直接 API，需 `ColorFilter.matrix`，先做纯 blur）
> - 对话框/底部面板用**半透明主题色**呈现玻璃卡片质感，未在内容内层嵌 `GlassContainer`（避免逐处改动与 blur 性能开销）
> - 卡片阴影通过 `elevation: 0` + 主题 surface 色差层次体现，未加投影
> 若验收效果不足，可在后续增强。

### Task 5: 底部导航栏玻璃化（main_screen.dart）

**Files:**
- Modify: `lib/main_screen.dart`
- Modify: `lib/features/gallery/gallery_screen.dart:611-617`（网格底部留白，供内容穿过玻璃导航栏）

- [ ] **Step 1: 修改 main_screen.dart**

顶部 import 追加：

```dart
import 'core/theme/glass_container.dart';
```

将 Scaffold 改为 `extendBody: true`，并把 `bottomNavigationBar` 包上玻璃容器：

```dart
      bottomNavigationBar: GlassContainer(
        blur: 22,
        borderRadius: 0,
        padding: EdgeInsets.zero,
        child: NavigationBar(
          selectedIndex: _currentTab,
          onDestinationSelected: (i) => setState(() => _currentTab = i),
          destinations: [
            NavigationDestination(
              icon: const Icon(Icons.photo_library_outlined),
              selectedIcon: const Icon(Icons.photo_library),
              label: s.tabGallery,
            ),
            NavigationDestination(
              icon: const Icon(Icons.search_outlined),
              selectedIcon: const Icon(Icons.search),
              label: s.tabSearch,
            ),
            NavigationDestination(
              icon: const Icon(Icons.settings_outlined),
              selectedIcon: const Icon(Icons.settings),
              label: s.tabSettings,
            ),
          ],
        ),
      ),
```

- [ ] **Step 2: gallery 网格底部留白（避免被玻璃导航栏遮挡）**

在 `lib/features/gallery/gallery_screen.dart` 的 `_buildMemeGrid` 中，将 GridView padding 改为：

```dart
          child: GridView.builder(
            padding: const EdgeInsets.fromLTRB(4, 4, 4, 84),
```

（search_screen 的网格同理，见 Task 7 Step 3）

- [ ] **Step 3: 选择模式底栏避让玻璃导航栏**

`extendBody: true` 使内层 gallery Scaffold 延伸到屏幕底部，选择模式的底栏（`_buildSelectionBottomBar`，第 436 行）会被外层玻璃导航栏（高 68）盖住，导出/复制/移入相册/删除按钮不可点击。修改其 Container padding，在底部留出导航栏高度：

```dart
    return Container(
      margin: const EdgeInsets.only(bottom: 68),
      padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 12),
      decoration: BoxDecoration(
```

> 说明：`margin: EdgeInsets.only(bottom: 68)` 让选择模式底栏整体上浮到玻璃导航栏上方；`SafeArea` 会自行处理系统手势条。

- [ ] **Step 4: 验证编译**

Run: `flutter analyze lib/main_screen.dart lib/features/gallery/gallery_screen.dart`
Expected: `No issues found!`

- [ ] **Step 5: 手动运行验证**

Run: `flutter run -d linux`
Expected: 底部导航栏呈毛玻璃，图库图片滚动可从其下方模糊穿过；进入选择模式后底栏浮于导航栏上方、按钮可点。

- [ ] **Step 6: Commit**

```bash
git add lib/main_screen.dart lib/features/gallery/gallery_screen.dart
git commit -m "feat(theme): 底部导航栏毛玻璃化"
```

### Task 6: gallery AppBar 玻璃化

**Files:**
- Modify: `lib/features/gallery/gallery_screen.dart`

- [ ] **Step 1: import 追加**

```dart
import 'dart:ui';
import '../../core/theme/app_animations.dart';
```

- [ ] **Step 2: Scaffold 启用 extendBodyBehindAppBar**

`build` 中的 Scaffold（第 293 行附近）追加 `extendBodyBehindAppBar: true`：

```dart
            Scaffold(
              extendBodyBehindAppBar: true,
              appBar: _selectionMode
                  ? _buildSelectionAppBar()
                  : _buildNormalAppBar(tabCount, nonDefaultAlbums),
```

- [ ] **Step 3: AppBar 玻璃包裹**

新增私有方法（放在 `_buildNormalAppBar` 之后）。注意玻璃层需带半透明底色，否则浅色主题/Tab 文字对比度不足：

```dart
  /// 将 AppBar 包上毛玻璃效果（配合 extendBodyBehindAppBar 使用）
  PreferredSizeWidget _glassWrap(PreferredSizeWidget appBar) {
    return PreferredSize(
      preferredSize: Size.fromHeight(appBar.preferredSize.height),
      child: ClipRect(
        child: BackdropFilter(
          filter: ImageFilter.blur(sigmaX: 22, sigmaY: 22),
          child: Container(
            decoration: BoxDecoration(
              color: Theme.of(context).brightness == Brightness.dark
                  ? Colors.white.withValues(alpha: 0.06)
                  : Colors.white.withValues(alpha: 0.7),
              border: Border(
                bottom: BorderSide(
                  color: Colors.white.withValues(alpha: 0.1),
                ),
              ),
            ),
            child: appBar,
          ),
        ),
      ),
    );
  }
```

在 `build` 中调用处改为：

```dart
              appBar: _selectionMode
                  ? _glassWrap(_buildSelectionAppBar())
                  : _glassWrap(_buildNormalAppBar(tabCount, nonDefaultAlbums)),
```

- [ ] **Step 4: body 顶部留白（内容从玻璃 AppBar 下穿过）**

在 `build` 顶部追加：

```dart
    const sortBarHeight = 36.0;
    const tabBarHeight = 48.0;
    final appBarBottomHeight = (tabCount > 1 ? tabBarHeight : 0.0) + sortBarHeight;
```

在类中新增成员变量（Step 4 使用，`_buildTabbedBody` 是类方法，不能用局部变量）：

```dart
  double _appBarOverlayTop = 0;
```

在 `build` 中、`_syncTabController` 之后赋值：

```dart
    _appBarOverlayTop = _selectionMode ? 56.0 : appBarBottomHeight;
```

修改 `_buildTabbedBody`，在其 Column 外层包一个顶部 Padding：

```dart
  Widget _buildTabbedBody(
    AsyncValue<List<Meme>> memeListAsync,
    List<Album> nonDefaultAlbums,
  ) {
    if (_tabController == null) return const SizedBox();
    ...
    return Padding(
      padding: EdgeInsets.only(top: _appBarOverlayTop),
      child: Column(
        children: [
          _buildAnalysisBanner(),
          Expanded(
            child: TabBarView(
              controller: _tabController,
              children: children,
            ),
          ),
        ],
      ),
    );
  }
```

**选择模式网格顶部偏移**：`_buildSelectionGrid` 复用 `_buildMemeGrid`，在 `extendBodyBehindAppBar` 下首行会被选择 AppBar（高 56）遮挡。修改 `_buildMemeGrid` 增加 `topPadding` 参数：

```dart
  Widget _buildMemeGrid(AsyncValue<List<Meme>> memeListAsync,
      {double? topPadding}) {
    ...
        return RefreshIndicator(
          onRefresh: ...,
          child: GridView.builder(
            padding: EdgeInsets.fromLTRB(4, topPadding ?? 4, 4, 84),
```

调用处：
- `_buildTabbedBody` 中 `_buildMemeGrid(memeListAsync, topPadding: _appBarOverlayTop)`
- `_buildSelectionGrid` 中 `_buildMemeGrid(AsyncValue.data(memes), topPadding: 56)`

> 注意：原 `_buildNormalAppBar` 内的 `totalBottomHeight` 常量保留使用，仅新增上层计算。

- [ ] **Step 5: 编译验证**

Run: `flutter analyze lib/features/gallery/gallery_screen.dart`
Expected: `No issues found!`

- [ ] **Step 6: 手动运行验证**

Run: `flutter run -d linux`
Expected: 图库页 AppBar（含 Tab 与排序栏）呈半透明毛玻璃，滚动图片可从其下方模糊穿过；切换选择模式后选择网格首行不被遮挡、AppBar 布局正常。

- [ ] **Step 7: Commit**

```bash
git add lib/features/gallery/gallery_screen.dart
git commit -m "feat(theme): 图库页 AppBar 毛玻璃化"
```

### Task 7: search AppBar 玻璃化

**Files:**
- Modify: `lib/features/search/search_screen.dart`

- [ ] **Step 1: import 追加**

```dart
import 'dart:ui';
import '../../core/theme/glass_container.dart';
```

- [ ] **Step 2: Scaffold 启用 extendBodyBehindAppBar + AppBar 玻璃 + 网格底部留白**

```dart
    return Scaffold(
      extendBodyBehindAppBar: true,
      appBar: PreferredSize(
        preferredSize: const Size.fromHeight(56),
        child: ClipRect(
          child: BackdropFilter(
            filter: ImageFilter.blur(sigmaX: 22, sigmaY: 22),
            child: Container(
              decoration: BoxDecoration(
                color: Theme.of(context).brightness == Brightness.dark
                    ? Colors.white.withValues(alpha: 0.06)
                    : Colors.white.withValues(alpha: 0.7),
                border: Border(
                  bottom: BorderSide(
                    color: Colors.white.withValues(alpha: 0.1),
                  ),
                ),
              ),
              child: AppBar(
                title: Text(s.search),
                actions: [
                  if (_hasActiveFilters)
                    TextButton(onPressed: _clearAll, child: Text(s.reset)),
                ],
              ),
            ),
          ),
        ),
      ),
```

body 的 Column 顶层包 Padding（顶部留出 AppBar 高度 56）：

```dart
      body: Padding(
        padding: const EdgeInsets.only(top: 56),
        child: Column(
          ...原有 children...
        ),
      ),
```

- [ ] **Step 3: 搜索结果网格底部留白（导航栏玻璃）**

`_buildResults` 中的 GridView padding 改为：

```dart
        padding: const EdgeInsets.fromLTRB(4, 4, 4, 84),
```

- [ ] **Step 4: 编译验证**

Run: `flutter analyze lib/features/search/search_screen.dart`
Expected: `No issues found!`

- [ ] **Step 5: 手动运行验证**

Run: `flutter run -d linux`
Expected: 搜索页顶部为毛玻璃（无标题文字），搜索框/色板/结果网格不被遮挡，图片滚动可从玻璃下方穿过。

- [ ] **Step 6: Commit**

```bash
git add lib/features/search/search_screen.dart
git commit -m "feat(theme): 搜索页 AppBar 毛玻璃化"
```

### Task 8: 回归验证

- [ ] **Step 1: 全量静态检查**

Run: `flutter analyze`
Expected: `No issues found!`（或仅存量 warning）

- [ ] **Step 2: 单元测试**

Run: `flutter test`
Expected: 全部 PASS（含新增 theme 测试）

- [ ] **Step 3: 集成测试抽查（Linux）**

Run: `flutter test integration_test/search_test.dart -d linux`
Expected: PASS

- [ ] **Step 4: 手动验收清单**

在 `flutter run -d linux` 下检查：
- [ ] 浅色/深色/跟随系统三种模式切换正常
- [ ] 图库网格卡片为 18px 圆角、实心表面色
- [ ] 底部导航栏毛玻璃，图片滚动从下方模糊穿过
- [ ] 图库/搜索 AppBar 毛玻璃，Tab 与排序栏正常可用
- [ ] 图库选择模式：底栏浮于玻璃导航栏上方、按钮可点；网格首行不被选择 AppBar 遮挡
- [ ] 各类 AlertDialog 为圆角半透明卡片
- [ ] 底部弹窗（导入预览）圆角 28
- [ ] 设置页卡片圆角统一，列表滚动到底不被玻璃导航栏遮挡

- [ ] **Step 5: 收尾提交**

```bash
git add -A
git commit -m "feat(theme): TREK 风格 UI 视觉改造完成" || echo "无新改动"
```

---

## Chunk 3: UI 标题优化 + OCR 开关 Bug 修复

> 背景：用户在计划进行中追加的三项需求
> - 搜索/设置 tab 顶部的大标题文字去除
> - Android/iOS 上 OCR 开关默认关闭、开启后重启即失效（bug）

**OCR Bug 根因（已排查）**：`OcrEnabledNotifier.build()`（`lib/features/gallery/gallery_provider.dart:790-822`）的平台分支遗漏了 Android/iOS。
Android/iOS 的 OCR 走 Google ML Kit（`lib/core/ocr/ocr_service.dart:56`），与 macOS Vision 一样是"系统级始终可用"，
但 build() 只有 macOS 分支读取已保存值，Android/iOS 掉到最后 `return false`，覆盖了已保存的 `ocr_enabled=true`，
导致每次启动强制关闭。修复：把 Android/iOS 并入"系统级 OCR 平台"分支，读取保存值默认 true。

### Task 9: 搜索页去除顶部标题

**Files:**
- Modify: `lib/features/search/search_screen.dart:150`

- [ ] **Step 1: 去掉 AppBar 标题**

将：

```dart
              child: AppBar(
                title: Text(s.search),
                actions: [
                  if (_hasActiveFilters)
                    TextButton(onPressed: _clearAll, child: Text(s.reset)),
                ],
              ),
```

改为：

```dart
              child: AppBar(
                actions: [
                  if (_hasActiveFilters)
                    TextButton(onPressed: _clearAll, child: Text(s.reset)),
                ],
              ),
```

- [ ] **Step 2: 编译验证**

Run: `flutter analyze lib/features/search/search_screen.dart`
Expected: `No issues found!`（`s` 变量仍在其他处使用，保留 import）

- [ ] **Step 3: Commit**

```bash
git add lib/features/search/search_screen.dart
git commit -m "feat(ui): 搜索页去除顶部标题文字"
```

### Task 10: 设置页去除顶部标题

**Files:**
- Modify: `lib/features/settings/settings_screen.dart:328`

- [ ] **Step 1: 去掉 AppBar 标题 + 列表底部留白**

将：

```dart
      appBar: AppBar(title: Text(S.of(context).settings)),
```

改为：

```dart
      appBar: AppBar(),
```

同时，`extendBody: true` 下设置页 ListView 最后一项会贴住玻璃导航栏，将 ListView padding 从 `EdgeInsets.all(16)` 改为底部留出导航栏高度：

```dart
      body: ListView(
        padding: const EdgeInsets.fromLTRB(16, 16, 16, 84),
```

- [ ] **Step 2: 编译验证**

Run: `flutter analyze lib/features/settings/settings_screen.dart`
Expected: `No issues found!`

- [ ] **Step 3: 手动运行验证**

Run: `flutter run -d linux`
Expected: 设置页顶部无标题、各项卡片正常，滚动到底时最后一项不被玻璃导航栏遮挡。

- [ ] **Step 4: Commit**

```bash
git add lib/features/settings/settings_screen.dart
git commit -m "feat(ui): 设置页去除顶部标题文字"
```

### Task 11: 修复 Android/iOS OCR 开关重启失效

**Files:**
- Modify: `lib/features/gallery/gallery_provider.dart:790-822`
- Test: `test/core/providers/ocr_enabled_notifier_test.dart`（新建）

- [ ] **Step 1: 编写失败测试**

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:mememaster/features/gallery/gallery_provider.dart';
import 'package:shared_preferences/shared_preferences.dart';

void main() {
  test('系统级 OCR 平台（Android/iOS）读取已保存的开关状态', () async {
    // 预置已保存状态：ocr_enabled = true
    SharedPreferences.setMockInitialValues({'ocr_enabled': true});
    final prefs = await SharedPreferences.getInstance();

    final container = ProviderContainer(
      overrides: [
        sharedPreferencesProvider.overrideWithValue(prefs),
        // 模拟 Android：platformCheck 返回 true（系统级 OCR）
        ocrEnabledProvider.overrideWith(
          () => OcrEnabledNotifier(platformCheck: () => true),
        ),
      ],
    );
    addTearDown(container.dispose);

    expect(container.read(ocrEnabledProvider), true);
  });

  test('非系统级平台且 OCR 不可用时强制关闭', () async {
    SharedPreferences.setMockInitialValues({'ocr_enabled': true});
    final prefs = await SharedPreferences.getInstance();

    final container = ProviderContainer(
      overrides: [
        sharedPreferencesProvider.overrideWithValue(prefs),
        ocrEnabledProvider.overrideWith(
          () => OcrEnabledNotifier(
            platformCheck: () => false,
            ocrAvailableCheck: () => false,
          ),
        ),
      ],
    );
    addTearDown(container.dispose);

    expect(container.read(ocrEnabledProvider), false);
  });
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `flutter test test/core/providers/ocr_enabled_notifier_test.dart`
Expected: FAIL（编译错误：`OcrEnabledNotifier` 无 `platformCheck` / `ocrAvailableCheck` 参数）

- [ ] **Step 3: 修改 `OcrEnabledNotifier` 使其可注入平台判断**

在 `lib/features/gallery/gallery_provider.dart` 中修改：

```dart
class OcrEnabledNotifier extends Notifier<bool> {
  OcrEnabledNotifier({bool Function()? platformCheck, bool Function()? ocrAvailableCheck})
      : _platformCheck = platformCheck,
        _ocrAvailableCheck = ocrAvailableCheck;

  /// 可注入的平台判断（测试用）；默认按真实 Platform 判断
  final bool Function()? _platformCheck;
  final bool Function()? _ocrAvailableCheck;

  @override
  bool build() {
    // 注入的 OCR 可用性检查（测试用）：显式不可用时强制关闭
    if (_ocrAvailableCheck?.call() == false) return false;

    // 系统级 OCR 平台（macOS: Apple Vision / Android·iOS: Google ML Kit）：
    // OCR 始终可用，直接读取用户已保存的开关状态，默认开启
    if (_isSystemOcrPlatform()) {
      return _getSavedEnabled() ?? true;
    }

    // Linux/Windows: 检查 FFI / CLI 是否可用
    final ffiLoaded = TessOcrBindings.ffiIsLoaded;
    debugPrint('[OCR] FFI loaded: $ffiLoaded');

    if (ffiLoaded) {
      return _getSavedEnabled() ?? true;
    }

    // Linux: FFI 未加载，尝试 CLI fallback
    if (Platform.isLinux) {
      try {
        final result = Process.runSync('tesseract', ['--version']);
        if (result.exitCode == 0) {
          debugPrint('[OCR] CLI Tesseract detected: ${result.stdout.toString().split('\n').first}');
          return _getSavedEnabled() ?? true;
        }
        debugPrint('[OCR] CLI Tesseract check failed: exitCode=${result.exitCode}');
      } catch (e) {
        debugPrint('[OCR] CLI Tesseract not found: $e');
      }
    }

    // FFI 和 CLI 都不可用 → OCR 无法使用
    return false;
  }

  /// 平台是否具备"系统级" OCR（无需额外安装即可使用）
  /// macOS: Apple Vision / Android·iOS: Google ML Kit
  bool _isSystemOcrPlatform() =>
      _platformCheck?.call() ??
      (Platform.isMacOS || Platform.isAndroid || Platform.isIOS);

  bool? _getSavedEnabled() {
    try {
      return ref.read(sharedPreferencesProvider).getBool('ocr_enabled');
    } catch (_) {
      return null;
    }
  }

  void setEnabled(bool value) {
    state = value;
    try {
      ref.read(sharedPreferencesProvider).setBool('ocr_enabled', value);
    } catch (_) {}
  }
}
```

> 说明：`_ocrAvailableCheck` 在 `build()` 顶部被消费（显式 false 时强制关闭），用于测试隔离"OCR 不可用"场景；Linux/Windows 探测逻辑保持不变，只新增 `_isSystemOcrPlatform()` 分支与已保存值的读取。

- [ ] **Step 4: 运行测试确认通过**

Run: `flutter test test/core/providers/ocr_enabled_notifier_test.dart`
Expected: PASS（2 个用例）

- [ ] **Step 5: 全量测试回归**

Run: `flutter test`
Expected: 全部 PASS

- [ ] **Step 6: Commit**

```bash
git add lib/features/gallery/gallery_provider.dart test/core/providers/ocr_enabled_notifier_test.dart
git commit -m "fix(ocr): Android/iOS OCR 开关重启后失效，系统级平台读取已保存状态"
```